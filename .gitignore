# LaTeX build artifacts (transient — re-creatable by latexmk)
# Final PDFs in build/ are tracked (see explicit !**/build/*.pdf below).
**/build/
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
*.lof
*.lot
*.bcf
*.run.xml
*-blx.bib
*.brf
*.idx
*.ind
*.ilg
*.glg
*.glo
*.gls
*.glsdefs
*.ist
*.nav
*.snm

# Editor temp
.DS_Store
*.swp
*.swo
~$*
*.tmp

# Pandoc temp
*.pandoc-aux

# Generated PDFs next to .tex are transient (use build/ for tracked)
conference/tex/main.pdf
journal/tex/main.pdf
supplementary/conference/*.pdf
supplementary/journal/*.pdf

# Track the final PDFs and DOCX so a reader does not have to run
# the build to inspect them
!conference/build/
!conference/build/main.pdf
!journal/build/
!journal/build/main.pdf
!journal_v_sweep/build/
!journal_v_sweep/build/main.pdf
!journal_backbone_factorial/build/
!journal_backbone_factorial/build/main.pdf
!journal_interpretability/build/
!journal_interpretability/build/main.pdf
!supplementary/build/
!supplementary/build/*.pdf
!conference/word/
!conference/word/main.docx
!journal/word/
!journal/word/main.docx
!supplementary/word/
!supplementary/word/**/*.docx

# Reference templates downloaded archives
templates/*.zip
templates/*.tar.gz

# Transient PDFs next to .tex for the new manuscript series (tracked copy lives in build/)
journal_v_sweep/tex/*.pdf
journal_backbone_factorial/tex/*.pdf
journal_interpretability/tex/*.pdf
figures/source/__pycache__/

# Python virtualenvs (any name, any depth) — never commit venvs
.venv/
.venv*
**/.venv*
venv/
**/venv/
